# Learning Log

- When recreating Dart files due to environment issues, always make sure to put them back exactly as requested with all features in place.
- Supabase needs `.single()` to return only a single object from query. If one wishes to receive an object instead of a list of objects, `.single()` should be used.
- Ensure that the tests are updated to match any structural refactoring or missing dependencies.
